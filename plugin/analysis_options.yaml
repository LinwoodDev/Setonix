analyzer:
  exclude:
    - rust/target/**.dart # contains dumped debug info, instead of normal code
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**

linter:
  rules:
    - unawaited_futures
