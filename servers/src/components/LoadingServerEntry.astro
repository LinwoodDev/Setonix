---
import type { Server } from "../scripts/servers/utils";
import ServerEntry from "./ServerEntry.astro";


interface Props {
  server: Server;
}

const { server } = Astro.props;
---

<ServerEntry serverStatus={true} server={server} server:defer>
  <ServerEntry slot="fallback" server={server} />
</ServerEntry>
