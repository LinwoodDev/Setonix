import 'package:flutter/material.dart';

class CreateOnlinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Create online game")),
      body: ,
    );
  }
}
